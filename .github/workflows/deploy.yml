name: Test API Prediction Sentiment

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: windows-latest

    steps:
    # Étape 1 : Configuration de l'environnement
    - name: Check out repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"

    # Étape 2 : Installer les dépendances
    - name: Install dependencies
      run: |
        python -m venv myenv
        .\myenv\Scripts\activate
        pip install --upgrade pip
        pip install -r requirements.txt

    # Étape 3 : Lancer les tests unitaires
    - name: Run FastAPI tests
      run: |
        .\myenv\Scripts\activate
        pytest test_API_prediction_sentiment.py --disable-warnings

    # Étape 4 : Lancer l'application Streamlit pour les tests manuels (optionnel)
    - name: Run Streamlit app (optional for manual testing)
      run: |
        .\myenv\Scripts\activate
        streamlit run test_API_prediction_sentiment.py
      continue-on-error: true
